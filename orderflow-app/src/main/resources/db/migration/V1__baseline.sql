-- Базовая миграция, чтобы проверить, что Flyway работает.
--
-- Правила:
--   * имя файла: V<номер>__<описание_через_подчёркивания>.sql
--   * применённую миграцию НИКОГДА не редактируем, только пишем новую
--   * схема меняется только через Flyway (spring.jpa.hibernate.ddl-auto=validate)

COMMENT ON SCHEMA public IS 'OrderFlow';
